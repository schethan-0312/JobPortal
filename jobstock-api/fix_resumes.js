import { PrismaClient } from './src/generated/prisma/index.js'; const prisma = new PrismaClient(); async function main() { const resumes = await prisma.candidateResume.findMany({ where: { resumeUrl: { not: null } } }); let count = 0; for (const r of resumes) { await prisma.candidateProfile.update({ where: { id: r.candidateId }, data: { resumeUrl: r.resumeUrl, skills: r.skills, experienceYears: r.experienceYears } }); count++; } console.log('Updated ' + count + ' profiles'); } main().catch(console.error).finally(() => prisma.$disconnect());
