const { PrismaClient } = require('./generated/prisma/index.js'); const prisma = new PrismaClient(); prisma.candidateProfile.groupBy({ by: ['location'], _count: { _all: true }, orderBy: { _count: { location: 'desc' } } }).then(console.log).finally(()=>prisma.$disconnect());
